import React from "react";
import ReactDOM from "react-dom";
import PhotoFinder from "./App";

ReactDOM.render(
  <React.StrictMode>
    <PhotoFinder />
  </React.StrictMode>,
  document.getElementById("root")
);
